---
layout: none
path: feed-01.xml
---

<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="es">
    <title type="text">{{ site.title | escape }}</title>
    <subtitle type="html">{{ site.description | escape }}</subtitle>
    <link href="{{ site.url }}/feed.xml" rel="self"/>
    <link href="{{ site.url }}/"/>
    <id>{{ site.url }}/</id>
    <author>
        <name>{{ site.author | escape }}</name>
    </author>
    <updated>{{ site.time | date_to_xmlschema }}</updated>

    {% for post in site.posts limit:10 %}
<entry>
    <title type="text">{{ post.title | escape }}</title>
    <link href="{{ post.url | absolute_url }}"/>
    <id>{{ post.url | absolute_url | remove: '.html' }}</id>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>

    <summary type="html">{{ post.excerpt | xml_escape | truncatewords: 30 }} **Para ver la publicación completa y todo nuestro contenido, haz clic en el enlace de nuestro perfil.**</summary>

    {% if post.author %}
        <author><name>{{ post.author | escape }}</name></author>
    {% endif %}

    {% if post.image %}
        <media:content xmlns:media="http://search.yahoo.com/mrss/"
            medium="image" 
            url="{{ site.url }}{{ site.baseurl }}/{{ post.image }}"
        />
    {% endif %}

    <content type="html">{{ post.content | xml_escape }}</content>
</entry>
{% endfor %}
</feed>